[package]
name = "openay-protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "OpenAY Mic wire protocol: packet encode/decode per shared/protocol.md"

[dependencies]

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
